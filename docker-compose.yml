version: '3'

services:
  db:
    # We use a mariadb image which supports both amd64 & arm64 architecture
    image: mariadb
    # If you really want to use MySQL, uncomment the following line
    #image: mysql:8.0.27
    command: '--default-authentication-plugin=mysql_native_password'
    volumes:
      - db_data:/var/lib/mysql
    restart: unless-stopped
    environment:
      - MYSQL_ROOT_PASSWORD=Darl!ng#123
      - MYSQL_DATABASE=techlurno
      - MYSQL_USER=techlurno
      - MYSQL_PASSWORD=Darl!ng#123
    expose:
      - 3306
      - 33060
  wordpress:
    image: wordpress
    ports:
      - 80:80
      - 443:443
    restart: unless-stopped
    environment:
      - WORDPRESS_DB_HOST=db
      - WORDPRESS_DB_USER=techlurno
      - WORDPRESS_DB_PASSWORD=Darl!ng#123
      - WORDPRESS_DB_NAME=techlurno
volumes:
  db_data: